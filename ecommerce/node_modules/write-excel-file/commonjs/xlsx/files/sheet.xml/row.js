"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = generateRow;
var _cell = _interopRequireDefault(require("./cell.js"));
var _getCellStyleProperties = _interopRequireDefault(require("./helpers/getCellStyleProperties.js"));
var _getOpeningTagMarkup = _interopRequireDefault(require("../../../xml/getOpeningTagMarkup.js"));
var _getClosingTagMarkup = _interopRequireDefault(require("../../../xml/getClosingTagMarkup.js"));
var _isCellObject = _interopRequireDefault(require("../../helpers/isCellObject.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { "default": e }; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// import Integer from '../types/Integer.js'

function generateRow(row, rowIndex, _ref) {
  var findOrCreateCellStyle = _ref.findOrCreateCellStyle,
    findOrCreateSharedString = _ref.findOrCreateSharedString,
    customFont = _ref.customFont,
    dateFormat = _ref.dateFormat,
    features = _ref.features,
    usesSchema = _ref.usesSchema;
  // To ensure the row number starts as in Excel.
  var rowNumber = rowIndex + 1;
  var rowHeight;
  var rowCells = row.map(function (cell, columnIndex) {
    if (cell === undefined || cell === null) {
      return '';
    }
    var cellObject = (0, _isCellObject["default"])(cell) ? cell : {
      value: cell
    };
    var cellStyleProperties = (0, _getCellStyleProperties["default"])(cellObject, features);
    var height = cellObject.height;
    var type = cellObject.type,
      value = cellObject.value,
      format = cellObject.format;
    if (isEmpty(value)) {
      value = null;
    } else {
      // Get cell value type.
      if (type === undefined) {
        if (!usesSchema) {
          type = detectValueType(value);
        }
        if (type === undefined) {
          // The default cell value type is `String`.
          type = String;
          value = String(value);
        }
      }
    }

    // Validate `format` property.
    if (format) {
      if (type !== Date && type !== Number && type !== String && type !== 'Formula') {
        // && type !== Integer) {
        throw new Error('`format` can only be used on `Date`, `Number`, `String` or `"Formula"` cells'); // or `Integer` cells')
      }
      if (type === String && format !== '@') {
        throw new Error('`String` cells only support "@" `format`');
      }
    } else {
      if (type === Date) {
        format = dateFormat;
      }
    }
    var cellStyleId;
    if (format || customFont || cellStyleProperties) {
      cellStyleId = findOrCreateCellStyle(_objectSpread({
        format: format
      }, cellStyleProperties));
    }
    if (height) {
      if (rowHeight === undefined || rowHeight < height) {
        rowHeight = height;
      }
    }
    return (0, _cell["default"])(rowNumber, columnIndex, value, type, cellStyleId, findOrCreateSharedString);
  }).join('');
  var rowAttributes = {
    r: rowNumber
  };
  if (rowHeight) {
    rowAttributes.ht = rowHeight;
    rowAttributes.customHeight = 1;
  }
  return (0, _getOpeningTagMarkup["default"])('row', rowAttributes) + rowCells + (0, _getClosingTagMarkup["default"])('row');
}
function isEmpty(value) {
  return value === undefined || value === null || value === '';
}
function detectValueType(value) {
  switch (_typeof(value)) {
    case 'string':
      return String;
    case 'number':
      return Number;
    case 'boolean':
      return Boolean;
    default:
      if (value instanceof Date) {
        return Date;
      }
  }
}
//# sourceMappingURL=row.js.map